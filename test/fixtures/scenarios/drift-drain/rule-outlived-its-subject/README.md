# edge-service

`src/edge` terminates the protocol; `src/domain` holds the model. The
`src/legacy/gateway` package this repository used to carry was removed once
the edge router took the last endpoint.
