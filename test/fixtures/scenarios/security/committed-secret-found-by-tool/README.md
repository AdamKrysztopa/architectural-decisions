# orders-api

A small internal orders service. gitleaks runs in CI; its recorded output for the
current default branch is in `ci/gitleaks-report.txt`.

The secret it found has NOT been rotated. Removing it from HEAD did not remove it
from history.
