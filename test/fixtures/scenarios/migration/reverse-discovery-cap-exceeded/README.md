# monorepo

Fourteen services under `services/`, one shared library under `libs/platform`.
Nothing here is written down: there is no `docs/adr/`, no
`docs/architecture/decisions/`, and no architecture notes of any kind.

The conventions are real and consistent -- they were simply never recorded.
