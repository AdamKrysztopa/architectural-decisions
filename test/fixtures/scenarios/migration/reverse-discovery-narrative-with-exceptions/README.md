# shop-core

A domain package with no written architecture decisions. `src/domain/` holds
the model, `src/application/` wires repositories, `src/infra/` holds the
database session and the HTTP client.

There is no `docs/architecture/decisions/` content yet: this repository is the
"undocumented repository" case that reverse discovery is pointed at.
